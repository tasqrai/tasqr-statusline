line-length = 100
target-version = "py39"

[lint]
select = ["E", "F", "W", "I", "UP", "B", "C4", "SIM", "RET", "PIE", "PERF", "RUF"]

[lint.per-file-ignores]
"tests/*" = ["E402"]  # the suite puts the repo on sys.path before importing it
